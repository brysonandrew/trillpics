
<!-- {!isFirstPosition && (
  <div
    className={clsx(
      classValue,
      'bottom-0',
    )}
    style={sharedStyle}
  >
    <Specifications
      key='checkout'
      name={name}
      src={src}
      onToggle={onToggle}
      form={form}
      {...passedProps}
    />
  </div>
)} -->

<!-- import { I } from '@brysonandrew/icons-i';
import { TIMES_ICON } from '@brysonandrew/icons-keys';
import { Pill } from '@components/decoration/Pill';
import type { FC } from 'react';

export const C: FC = () => {
  return (
    <div className='absolute inset-y-6 inset-x-3 border-1-gray-02'>
      <Pill
        key='exit'
        classValue='absolute top-6 right-6 pointer-events-none'
        gradient='bg-fuchsia-pink-rose'
        isCircle
      >
        <I icon={TIMES_ICON} />
      </Pill>
    </div>
  );
}; -->
