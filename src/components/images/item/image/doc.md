
<!-- 
      // style={{
      //   ...imageProps.style,
      //   filter: `invert(${
      //     (canvas === 'white' &&
      //       dm.isDarkMode) ||
      //     (canvas === 'black' &&
      //       !dm.isDarkMode)
      //       ? 100
      //       : 0
      //   }%) brightness(${
      //     dm.isDarkMode ? 100 : 100
      //   }%)`,
      // }}
            {/* <Container
        uniqueId={uniqueId}
        isShown={Boolean(
          !isFirstPosition ||
            (isFirstPosition &&
              isAnyHover),
        )}
        isHover={isCheckoutHover}
        isParentHover={isHover}
        isFirstPosition={
          isFirstPosition
        }
        style={imageProps.style}
        onToggle={onToggle}
        form={form}
        {...passedProps}
      /> */} -->