import { Button } from '@brysonandrew/dark-mode';
import { FC } from 'react';

export const DarkMode: FC = () => {
  return (
    <Button
      iconProps={{
        classValue: 'w-9 h-9',
      }}
    />
  );
};
