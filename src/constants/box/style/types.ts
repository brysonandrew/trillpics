export type TBoxStyleTheme = {
  borderRadius: number;
  floating: {
    boxShadow: string;
  };
  flat: {
    boxShadow: string;
  };
  size: {
    md: string;
    sm: string;
    minWidth: string;
    minHeight: string;
  };
};
