import type { FC } from "react";

export const AddedPlaceholder: FC = () => {
  return (
    <div>
      {/* <div
              key={resolveCompositeKey(
                "placeholder",
                name
              )}
              className={clsx(
                "bg-black-05 center"
              )}
              style={style}
            >
              <img
                className="fill object-contain grayscale-100 mix-blend-difference opacity-50"
                src={resolvePicSrc(
                  name
                )}
                alt={name}
                style={style}
              />
            </div> */}
    </div>
  );
};