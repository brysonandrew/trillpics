import { DarkMode } from '@main/shell/header/dark-mode';
import { Header as _Header } from '@main/shell/header';

export const Header = () => {
  return (
    <_Header>
      <DarkMode />
    </_Header>
  );
};
