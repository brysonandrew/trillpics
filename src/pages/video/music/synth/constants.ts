export const SYNTH_TYPES_RECORD = {
  options: "options",
  multi: "multi",
} as const;
export const MUSIC_OPTIONS_RECORD = {
  bpm: "bpm",
  scaleKey: "scaleKey",
} as const;

export const INPUT_PATH_DELIMITER = ".";
