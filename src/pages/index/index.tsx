import { Collection } from '@components/collection';
import { Inits } from '@components/collection/variants/Inits';

export const Index = () => {
  return (
    <Collection>
      <Inits />
    </Collection>
  );
};
