import type { FC } from "react";
import { Seperator } from "~/pages/directors-mode/footer/controls/seperator";

export const SeperatorHorizontal: FC =
  () => (
    <Seperator sizeKey="minWidth" />
  );
