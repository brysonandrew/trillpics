
export type TBoxStyleTheme = {
  borderRadius: number;
  floating: {
    boxShadow: string;
  };
  flat: {
    boxShadow: string;
  };
  size: {
    s: string;
    m: string;
    minWidth: string;
    minHeight: string;
  };
};
