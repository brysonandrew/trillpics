import { callApi } from "./call-api";

// export const unsubscribeFromFileExistenceWatcher = ({ file, clientId, }) => {
//     return callApi('/api/unsubscribe-from-file-existence', { file, clientId });
// };
// export const subscribeToFileExistenceWatcher = async ({ file, clientId, }) => {
//     const { exists } = await callApi('/api/subscribe-to-file-existence', {
//         file,
//         clientId,
//     });
//     return exists;
// };